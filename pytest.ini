[pytest]
# pytest-homeassistant-custom-component needs asyncio auto mode; it is ignored
# when only plain pytest is installed, so the pure-logic tests in tests/ still
# run in a bare environment (tests/conftest.py skips tests/ha there).
asyncio_mode = auto
testpaths = tests
