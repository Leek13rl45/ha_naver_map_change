# The old apply / restore services are gone: no file is ever patched, so the
# concept no longer exists (docs/03-REDESIGN-SPEC.md section 3.9).
# Names and descriptions live in translations/{en,ko}.json under "services".
# The core-only build-time translation file must not be used by a custom
# integration (docs/02-HA-PLATFORM-2026.md section 4.5).
refresh_version:
  fields: {}

clear_cache:
  fields: {}
